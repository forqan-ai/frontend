[build]
  command = "npm run build"
  publish = "dist/Forqan/browser"

[build.environment]
  NETLIFY_SKIP_ANGULAR_RUNTIME = "true"

# Angular SPA routing - redirect all paths to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
